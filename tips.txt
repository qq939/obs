# VS Code Tips for Mac

1. **IntelliSense / Code Completion**:
   - Trigger suggestion: `Ctrl + Space` (or just type and wait)
   - Accept suggestion: `Tab` or `Enter`

2. **Navigation**:
   - Go to file: `Cmd + P`
   - Go to symbol: `Cmd + Shift + O`
   - Go to definition: `F12` or `Cmd + Click`

3. **Editing**:
   - Comment/Uncomment line: `Cmd + /`
   - Multi-cursor: `Option + Click`
   - Move line up/down: `Option + Up/Down`
   - Copy line up/down: `Shift + Option + Up/Down`

4. **Terminal**:
   - Toggle terminal: `` Ctrl + ` ``

5. **Emmet 快速补全（HTML/CSS）**:
   - 输入 `div.video-container` 按 `Tab` → 展开为 `<div class="video-container"></div>`
   - 输入 `ul>li*3` 按 `Tab` → 一次生成 3 个 li
   - 输入 `m10` 按 `Tab`（CSS 文件中）→ 展开为 `margin: 10px;`

6. **调试视频播放速率（本项目 -3x 倒放）**:
   - Chrome DevTools `Console` 里直接执行 `document.querySelector('video').playbackRate`
     查看当前速率；`= -3` 会让 Chrome 抛 `NotSupportedError`（故代码走兜底 seek）。
   - 想看原生负速率效果：Safari → 开发 → 显示网页检查器 → 控制台执行
     `document.querySelector('video').playbackRate = -3`。
   - 快速重跑集成测试：终端按 `Cmd + P` 输入 `task` 可跑 Task，或直接
     `python3 test_integration.py`（项目根目录）。

7. **抽象全局常量（本项目 url_head）**:
   - VSCode 里按住 `Cmd` 点击 `url_head` → 跳转到第 9 行定义处，按 `Shift + F12`
     可列出全部引用位置（对应 server.py 顶部注释里标注的行号）。
   - 批量改字符串：先手动抽成常量，再用 `Cmd + Shift + H`（全局替换）确认无残留；
     本项目用 `python3 test_url_head.py` 自动校验「无残留硬编码 URL」。
   - 想在文件内快速跳到顶部常量：`Cmd + Home`（macOS 为 `Cmd + ↑`）。

8. **快速 code snippet / 补全**:
   - 输入几个字母按 `Tab` 触发 Emmet（仅 HTML/CSS）或用户代码片段（snippets）。
   - 自定义片段：`Cmd + Shift + P` → 输入 `snippets` → 选
     `Preferences: Configure User Snippets` → 选 `python.json` / `javascript.json`，
     例如给 `def` 配 "def 函数模板"，之后输入 `def` 按 `Tab` 直接展开。
   - 触发参数提示 `Cmd + Shift + Space`；快速修复（导入缺失包等）`Cmd + .`。
   - 重命名符号全项目生效：`F2`。

9. **调试「手机/局域网 http 上传失败」（本项目 crypto.subtle 坑）**:
   - 浏览器只在**安全上下文**（https 或 localhost/127.0.0.1）才提供 `crypto.subtle`；
     用 `http://<局域网IP>` / `http://obs.dimond.top` 打开时它在控制台里是 `undefined`。
   - 一行自检：Chrome DevTools Console 执行
     `({secure: isSecureContext, subtle: typeof crypto.subtle})`；
     `secure:false, subtle:"undefined"` 即命中该问题（本项目的分片哈希兜底会自动接管）。
   - 本项目已加纯 JS SHA-256 兜底 `sha256HexJS`：http 下 `sha256Hex("abc")` 仍应等于
     `ba7816bf8f01cfea414140de5dae2223b00361a396177a9cb410ff61f20015ad`。
   - 看服务端日志：容器已挂载 `logs/`，直接 `tail -f logs/server.log`
     （VSCode 里 `Cmd + P` 打开 `logs/server.log` 也可实时刷新）。
   - 重建并重启：`docker compose up -d --build`（改 server.py 后必须重建镜像才生效）。

10. **调试「切页/切源后视频暂停」（本项目 ensurePlaying 保活）**:
   - 一行自检：DevTools Console 执行
     `(()=>{const v=document.querySelector('#videoContainer video');return {paused:v.paused,muted:v.muted,rate:v.playbackRate}})()`；
     `paused:true` 就是被暂停了。
   - 切页观察：`ArrowLeft/ArrowRight` 切换三页，每次切完再跑上一行，
     `paused` 应始终为 `false`（本项目在 `setPage()` 里强制 `playing = true` + `ensurePlaying()`）。
   - 自动播放策略：未静音 `play()` 在无用户手势时会被拒（`NotAllowedError`），
     `ensurePlaying()` 会自动静音重试；看到 `muted:true` 但 `paused:false` 属正常保活。
   - 注意：**左右滑动翻页（finishSwipe）也必须走 setPage**，否则会绕过保活逻辑。
   - `Cmd + F2` / `Shift + F12`：找 `ensurePlaying` / `setPage` 的全部引用。
   - 跑本任务测试：`python3 test_arrow_keys_swap.py`（含 node 行为断言 + 源码/线上校验）。

11. **VSCode 定位函数引用（本项目排查经验）**:
   - `Shift + F12`（Find All References）列出 `setPage` / `ensurePlaying` 所有调用点。
   - `Cmd + T`（Go to Symbol in Workspace）直接搜函数名跳转。
   - `Option + Shift + O`（Go to Symbol in File）在当前文件内按函数名跳转。
   - `Cmd + Shift + F` 全局搜索正则（勾选 `.*` 开启 RegExp），例如搜
     `video\.play\(\)\.catch\(\(\)\s*=>` 找出所有「无兜底的裸 play()」。

12. **本项目键盘快捷键速查 / 快速改键**:
   - 三页切换：`ArrowLeft` = 往信息页(0)方向；`ArrowRight` = 往设置页(2)方向
     （项目已按需求互换过，见 `case 'ArrowLeft' / 'ArrowRight'`）。
   - 切视频：`ArrowUp` 上一个 / `ArrowDown` 下一个；`Space` 播放/暂停。
   - 想快速互换两个 case：VSCode 里选中一个 `case` 整块 → `Option + ↑/↓` 上下移动
     → 再改 `setPage(currentPage ± 1)`；改完用 `Cmd + Shift + F` 搜 `setPage(currentPage`
     确认两处都已同步，避免漏改边界守卫 `if (currentPage <= 0 / >= PAGE_COUNT - 1)`。
   - 快捷键不受输入框影响：`keydown` 监听里已有 `INPUT/TEXTAREA/isContentEditable` 过滤。

13. **本项目 CSS 竖排布局改法（第三页「播放速度」为例）**:
   - 让「标签一行 + 每个选项一行」：父容器加 `flex-direction: column`，
     选项容器也 `flex-direction: column`，选项本身 `width: 100%; box-sizing: border-box`。
   - 在 DevTools 里快速验证是否真的分行：Elements 面板选中每个按钮，
     看 `getBoundingClientRect().top` 是否互不相同（本项目实测 439 / 470 / 502）。
   - 想临时预览改动：DevTools → Sources → 对应 css 文件直接改，满意后再落到源码，
     然后 `docker compose up -d --build` 重建（静态文件在镜像里，必须重建）。
   - 跑本任务测试：`python3 test_arrow_keys_swap.py`、`python3 test_speed_vertical_layout.py`。

14. **排查后端「卡顿」的通用套路（本项目 /videos 阻塞事件循环为例）**:
   - 先用 `curl -w "%{time_total}\n" -o /dev/null <url>` 量每个接口耗时，找出慢的那一个
     （本项目 `/videos` 曾 2.3~3.1s）。
   - 再验证「是否阻塞事件循环」：并发打 N 个慢接口，同时发一个轻量接口（如 `/health`），
     看轻量接口是否被拖慢。本项目实测 `/health` 从 0.009s → **17.68s**，即确诊阻塞。
   - 根因模式：`async def` 路由里**同步调用**阻塞函数（`subprocess.run` / `time.sleep` / 大文件 IO）
     → 单线程事件循环被占满。修法：`await asyncio.to_thread(阻塞函数)`。
   - 重复计算要加缓存：本项目给 ffprobe 时长加了 `(路径, size, mtime_ns, HLS签名)` 为 key
     的进程级缓存，热缓存后 `/videos` 从 2.5s → 0.009s。
   - 冷启动也要快：用 `ThreadPoolExecutor` 并发跑未命中缓存的探测（4 路并发，
     28 次串行 ffprobe → 约 1s）。
   - 快速定位同步阻塞点：`Grep` 搜 `subprocess.run` / `time.sleep` / `open(` 是否出现在
     `async def` 路由体内。
   - 跑本任务测试：`python3 test_videos_concurrency.py`（含重启容器后的冷启动并发验证）。

15. **在 FastAPI 进程内做一个「每天固定时刻」的定时任务（本项目 04:00 生成 HLS）**：
   - 不引 APScheduler/Celery，用「后台协程 + 轮询 + 日期键去重」就够了：
     `lifespan` 里 `task = asyncio.create_task(loop())`，`yield` 之后 `task.cancel()` 并
     `await` 它（`with suppress(asyncio.CancelledError)`），否则容器重启会留悬挂任务。
   - **关键：日期键去重**。轮询是每 30s 一次，如果只判断「现在是不是 04:00」，
     30 秒窗口内会触发多次。做法：命中时返回 `'YYYY-MM-DD'` 字符串存到全局 `_last_key`，
     下次同一天直接跳过。这样即使容器在 04:00 之后重启，当天也不会重复跑。
   - 时区别用 `datetime.utcnow().hour == 4`（那是 UTC）。用 `zoneinfo.ZoneInfo("Asia/Shanghai")`
     做 `now.astimezone(tz)`；容器里可能没装 tzdata，要 `try/except` 回退固定 `timezone(timedelta(hours=8))`。
   - 可测试性：把「判断是否命中」抽成纯函数 `cron_key(now, tz, hour, minute)`，
     测试时**直接注入不同时间**验证边界（04:00 命中 / 03:59、04:01 不命中），
     不用真等到凌晨 4 点，也不用 mock 时钟。
   - 生成类任务要**幂等 + 原子**：产物先写临时目录 `.tmp-xxx` 再 `os.replace` 到最终目录，
     避免 ffmpeg 中断留下半成品；再配一个 `meta.json`（version+源文件 size）判断是否过期，
     这样「重复调用不重跑」既省时间又能被测试断言。
   - 别让定时任务和线上请求抢资源：`sweep` 用**串行**逐个跑 ffmpeg（不要并发多路），
     每个文件一把 `asyncio.Lock` 防重入。

16. **Python 踩坑：`from datetime import time` 会遮蔽 `import time` 模块**：
   - 症状：`AttributeError: type object 'datetime.time' has no attribute 'time'`
     （写 `int(time.time()*1000)` 时炸）。
   - 原因：`datetime` 模块里也有个 `time` 类，`from datetime import datetime, time, ...` 把它
     绑定到了 `time` 这个名字上，后面 `import time` 的模块名就被覆盖了（后 import 的才生效，
     若 import 顺序在 from 之前则被覆盖）。
   - 修法：**不要** `from datetime import time`；需要时间戳就 `import time` 用 `time.time()`，
     需要时间类型就 `datetime.time(...)` 全限定名。
   - 快速自查：`grep -n "from datetime import" xxx.py`，看见 `time` 就警惕。
   - 单元测试里让函数 `print` 出中间量（如 `print('MADE', made)`），
     断言关键子串，比只看 traceback 更快定位是哪一步炸的。

17. **拿到旧代码做参考（本项目从 git 历史挖回 HLS 实现）**：
   - 二合一重构常把功能「弄丢」。想找回来先翻历史：`git log --oneline --all -- <关键字>`，
     再 `git show <commit>:<path>` 看当时的文件内容，比自己重写快且稳。
   - 本项目用了 `git show 5f15d64:<path>/server.js` 挖出旧 `obs-video-app` 的
     `generateHls/buildHlsArgs/canRemux/detectCodecs/detectRotation/hlsExists` 与每日 cron
     的完整逻辑，逐条搬成 Python。
   - macOS 小技巧：`cat -A` 是 GNU 的，macOS 自带 `cat` 没有 `-A`，
     看不可见字符用 `cat -v` 或 `sed -n l`。

